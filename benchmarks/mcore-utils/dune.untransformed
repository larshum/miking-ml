(env
  (dev
    (flags (
:standard -w -a 
))
    (ocamlc_flags (-without-runtime))))
(executable
  (name program)
  (libraries 
boot owl
)
  (link_flags -I 
%%PWD%%
 -cclib -lmexpr-cuda -cclib -lcudart -cclib -lstdc++))
